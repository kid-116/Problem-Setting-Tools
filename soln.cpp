#include <bits/stdc++.h>

using namespace std;

int main(int argc, char** argv) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    const string id = argv[1];
    string infile = "./tests/inputs/in" + id + ".txt";
    string outfile = "./tests/outputs/out" + id + ".txt";
    std:ifstream cin(infile);
    std::ofstream cout(outfile);
    // Your solution here
    
    return 0;
}